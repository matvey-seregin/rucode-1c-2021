
Функция PostInvoice(Date, Foto)
	
	ИмяПользователя = ИмяПользователя();
	
	ТекущийСотрудник = Справочники.Сотрудники.НайтиПоРеквизиту("ИмяПользователя", ИмяПользователя);
	
	Если Не ЗначениеЗаполнено(ТекущийСотрудник) Тогда
		Возврат "Неизвестное имя пользователя";
	КонецЕсли;
	
	ДокОбъект = Документы.ЧекЗаПитание.СоздатьДокумент();
	ДокОбъект.Дата = Date;
	ДокОбъект.ФотоЧека = Foto;
	ДокОбъект.Сотрудник = ТекущийСотрудник;
	ДокОбъект.Записать();
	
	Возврат "";
	
КонецФункции
